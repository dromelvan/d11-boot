spring:
  main.banner-mode: "off"
  flyway:
    locations: classpath:db/migration/schema,classpath:db/migration/view,classpath:db/migration/procedure/h2,classpath:db/migration/test-data/spring

d11:
  download.selenium:
    remoteUrl: http://localhost:4444/wd/hub
    # Provide user data dir only if using a standalone application Selenium
    userDataDir: .chrome
