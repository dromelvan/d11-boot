spring:
  flyway:
    schemas: development
  jpa:
    properties:
      hibernate:
        generate_statistics: true
        default_schema: development
        # format_sql: true
    # show-sql: true
